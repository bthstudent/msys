<?php
    $DBH = new DB();

    $DBH->query("CREATE TABLE IF NOT EXISTS settings (
      id int(11) NOT NULL AUTO_INCREMENT,
      option_name varchar(20) NOT NULL UNIQUE,
      option_value varchar(20) NOT NULL,
      PRIMARY KEY (id)
    ) ENGINE=MyISAM  DEFAULT CHARSET=utf8");
    $DBH->execute();

    $DBH->query("INSERT INTO settings(option_name, option_value) VALUES('db-version', 0)");
    $DBH->execute();
?>